<?xml version="1.0" encoding="utf-8"?>
<!-- GeneralsX @feature Android port launcher-ui-2026 08/09/2026 Rounded
     panel behind the launcher dialogs. GeneralsX @bugfix Android port
     accent-tinted-surfaces 21/09/2026 The launcher's dialogs are all
     MaterialAlertDialogBuilder now — they resolve this drawable's colour
     through the dialog theme, so an accent overlay tints the panel (and
     every launcher dialog) with the picked accent instead of staying on the
     default palette. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/gzh_surface_container_high" />
    <corners android:radius="28dp" />
</shape>
