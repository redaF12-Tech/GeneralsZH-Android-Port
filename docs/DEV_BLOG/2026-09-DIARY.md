# Development Diary - September 2026

---

## 2026-09-19: A tester's LAN "create game" crash, and why it took a day to name

A tester reported that singleplayer worked perfectly while pressing "create
game" in the LAN lobby -- and in online -- closed the app instantly. They were
told to try the OpenGL backend instead of Vulkan; that was never going to help.

The engine log ended between two of `Shell::doPush()`'s own prints:

```
DEBUG: About to call TheWindowManager->winCreateLayout('Menus/LanGameOptionsMenu.wnd')
DEBUG: winCreateLayout returned: 0xb400007c56c020f0
<crash>
```

so the layout had loaded and the crash was inside `runInit()`. `crash.log` gave
`signal=11 fault_addr=0x8` and a backtrace of bare offsets.

Symbolized, it is:

```
crash PC      GameWindow::winGetEnabled() + 0x0
crash LR      PopulateStartingCashComboBox(GameWindow*, GameInfo*) + 0x1b0
backtrace[0]  InitLanGameGadgets() + 0x30c
backtrace[1]  LanGameOptionsMenuInit(WindowLayout*, void*) + 0xd4
backtrace[2]  Shell::doPush(AsciiString) + 0x168
```

`GameWindow` derives from `MemoryPoolObject`, which contributes a vptr and no
data, so on arm64 its first member `m_status` sits at +0x8 -- and
`winGetEnabled()` reads that and nothing else. `fault_addr=0x8` is a null
`GameWindow*`, exactly.

The null came from `ComboBoxStartingCash`, and the reason nothing complained is
that `winCreateLayout` does not fail on a control it cannot build:
`GameWindowManagerScript.cpp:2899-2905` stores whatever `parseWindow` returned,
`nullptr` included, and carries on to the next control. The lookups were all
guarded by `DEBUG_ASSERTCRASH`, which `Debug.h:206` compiles to `((void)0)` in
the shipped RelWithDebInfo build. So the layout loads, one control is missing,
and the first line that dereferences a gadget takes the process down.

Fixing that one pointer would have moved the crash three lines down; there are
more than twenty unguarded dereferences in that function alone, plus their twins
on the online screen. So instead: `InitLanGameGadgets` and `InitWOLGameGadgets`
now look everything up first, print the name of every control that is missing,
and answer `FALSE`; the screen declines to open and the player keeps a running
game and a readable log. Same bail-out `Shell::doPush()` already performs when
`winCreateLayout` itself returns nothing -- that guard just never covered a
layout that loads with holes in it. `PopulateStartingCashComboBox`, which all
three setup screens (LAN, online, skirmish) reach, guards itself too, since
"a null here kills the game" should not depend on every caller remembering.

Three of the online screen's asserts were also checking the wrong pointer --
`windowMap` was asserted four times in a row, standing in for
`checkBoxLimitSuperweapons`, `comboBoxStartingCash` and `checkBoxLimitArmies` --
so even a debug build would not have caught those.

### The part that actually cost the day

None of the above needed a device. Naming the functions did, and the crash log
could not do it: it printed `libmain.so+0x11f5268` and nothing else, although
the very same log contains

```
[texchurn] create #33 ... nearest_sym=_ZN21Render2DSentenceClass14Build_TexturesEv
```

-- the process can name its own addresses. `--strip-unneeded` removes `.symtab`
and the DWARF but leaves `.dynsym`, the engine builds with default visibility,
and the version script only hides `operator new`/`delete`, so `dladdr` resolves
ordinary engine functions in the shipped APK. The crash handler simply never
asked. It now does, after writing the offset-only line, so a `dladdr` that
deadlocks or faults costs the names and never the report -- the same
fault-tolerance trade the frame-pointer walk already makes.

Worse, the log's `[build compiled Aug 30 2026 16:38:03]` stamp was wrong.
ccache replays a cached object file with the preprocessor date baked into it,
which `GameEngine.cpp:858-867` already warns about; the only library whose code
layout agreed with the addresses in that log was built on 6 September.
Establishing that meant searching every APK committed under `apk/` for one whose
symbol table put a known offset inside a known function. The linker's build id
settles it in one field, so the crash log and the startup banner now carry it,
read out of our own mapped ELF image at load, alongside the CI run number that
names the artifact.

And the CI symbol bundle that would have answered this in a minute had expired:
`retention-days: 7`, copied from the APK step, where the 0.5 GB quota argument
genuinely applies. It does not apply to a few MB of gzipped text, and seven days
is shorter than the round trip on a tester report. Now 90.

### Not reproducible locally, and that is the point

The same sequence on the current build does not crash here. The LAN menu code
has not changed since March, so this is not something that was fixed in the
meantime -- the tester runs a Russian repack whose `WindowZH.big` presumably
lacks, or fails to parse, that one control. Which is precisely why the fix is
"the log names the missing control" rather than "add the control": we cannot see
their data, and the next tester will have different data again.

---

## 2026-09-20: The desync was never arithmetic

For weeks the cross-play desync was chased as a floating-point problem: arm64
does not compute what x86 computes, therefore lockstep cannot hold. A Windows
build was brought up and abandoned in service of that theory. It was the wrong
theory, and two measurements plus one overlooked directory ended it.

**Measurement.** The twelve transcendental expressions from `SimulationMathCrc`
were compiled for x86_64 and for aarch64 against the same glibc and run (aarch64
under qemu-user). All twelve agree bit for bit, in single and in double
precision, and so does the pathfinder's `10*sqrt(dx²+dy²)` followed by a cast to
int. Architecture is not the divergence. `-ffp-contract=off` is doing its job.

**The directory.** `/home/user/generalsonlinedevelopmentteam/gameclient` holds
the full source of the PC client we are trying to agree with. The problem was
never numerical analysis; it is a diff.

The diff found nine divergences. Three are live and move the logic RNG stream,
which is the quantity a lockstep desync amplifies fastest. All three are fixed
here, each verified line by line against the client rather than inferred:

`InstantDeathBehavior::onDie` ran its dead-state check first; the client runs it
last. When an object with AI is already dead, the client still draws for the fx
and ocl lists and then returns, while we returned having drawn nothing. Two
machines that disagree about how many numbers were taken from the logic RNG
cannot stay in sync. The same function also fired death weapons on scaffolds
under construction, which the client does not -- extra damage, extra objects,
another draw, on an action players take in the first seconds of a match.

`EMPUpdate.cpp` drew a particle's vertical offset from the logic RNG where the
client draws from the client RNG. `emitterCount` is at least fifteen, so every
EMP-disabled victim pulled fifteen-plus extra numbers out of the simulation
stream on our side and none on theirs. The comment a few lines below, which
correctly explains that the streams differ for `setInitialDelay`, reads as
though it covers this line too; that is probably why it survived.

`HelixContain::removeFromContain` lost its braces and a function call to a
`#if RETAIL_COMPATIBLE_CRC / #else / #endif` strip. What remained was a
brace-less `if` guarding only an assignment, and no `friend_setContainedBy`
call at all -- so a portable structure kept a dangling `m_containedBy` and
never cleared `m_containedByFrame`, which `Object::crc` hashes directly. A
mechanical port bug that put a divergence straight into the checksum.

The remaining six are recorded for follow-up: sniper-vs-structure damage and
the Aurora fuel bomb in `Weapon.cpp`, `MinefieldBehavior` destroying an object
the client keeps alive, and two of our own POSIX bugfixes (`OverlordContain`,
`ParkingPlaceBehavior`) that move CRC-hashed position state and so need a
version that does not.

Two conclusions worth keeping. The `USE_DETERMINISTIC_MATH` scaffolding in
`wwmath.h` should not be switched on: the PC client does not use GameMath or
fdlibm, so routing our side through them would replace a divergence measured at
approximately zero with an unmeasured one. And the existing
`(float)atan2((double)y, (double)x)` pattern is already bit-exact with what the
client's x87 produces at 24-bit precision -- "fixing" it to `atan2f` would
introduce disagreement on unit facing where there is none today.

Android played Android cleanly all along because both Androids share our
source. Android and the PC disagreed because our source and theirs describe
different simulations.

## 20/09/2026 — The desync measurement was off by one checkpoint

The user sent the map pack they had been testing on (`Casino - Resurrection`,
nine variants). Decompressing a `.map` is one screen of Python — `EAR\0` header,
RefPack stream, then the `CkMp` chunk tree whose layout is spelled out in
`DataChunk.cpp` and `Scripts.cpp` — so for the first time the scripts that run in
the first hundred frames could be read instead of guessed at.

What the map does explains why this one map was the one that failed. Group
`CounterRandom` holds two scripts, `Randomizer` and `Randomizer2`, neither of them
one-shot, each of which calls `SET_RANDOM_TIMER` on both the true and the false
branch. They run every logic frame, so the map draws from the logic RNG twice per
frame from frame 0 onwards — which is exactly the `ScriptEngine.cpp:6769 drew 200`
in the user's frame-100 tally, two draws times a hundred frames. A map that pumps
the shared random stream every frame amplifies any divergence immediately; a still
map hides one for a thousand frames. That was the whole of the "map-specificity"
worry, and it is a property of the map, not of a bug per map.

With that settled, the frame-0 object dump was worth a second look, and it said
something the earlier reading had skated over: twelve `SupplyPileSmall`, in six
pairs at identical positions, two of them at `0,0,0`. Six are the map's own
`Supply1..6`; six are `SupplyCasibo1..6`, which the `Supply Spawn` script creates
at `WaypointSpawnSupplyP0..P5` — and on a 2v2 map the last two waypoints do not
exist, hence the origin. Symmetric on both sides, so not the fault, but it did
prove the scripts had already run by the first checkpoint.

### The actual finding

The instrument was wrong. `RecorderClass::CRCInfo` on the PC client carries a flag
this port had dropped:

```cpp
m_skippedOne = !isMultiplayer;        // constructor
...
if (!m_skippedOne) { m_skippedOne = TRUE; return; }   // addCRC
```

In a network game the first `MSG_LOGIC_CRC` never makes it onto the wire, so a
multiplayer recording's checksum stream starts one interval later than the local
stream does during playback. The client compensates by discarding this device's
first local checksum. Without that discard every comparison is shifted by one
checkpoint, which produces precisely the two results this port has been reporting:
a mismatch at the very first comparison on a map whose state changes between
checkpoints, and ten consecutive perfect matches on a map where nothing moves.

A frame-matching scheme had been written here in September to avoid having to
guess whether the recording's first checksum survived. The guess was never needed.
The recording states its own game mode in its header, and the client reads it:

```cpp
Bool isMultiplayer = (m_originalGameMode == GAME_INTERNET || m_originalGameMode == GAME_LAN);
```

This port derived the same flag from `getSlot(localPlayerIndex)->getIP() != 0`,
because `CRCInfo` was constructed a few lines *before* the header fields carrying
the mode were read. So the one input that decides the pairing was a stand-in for a
value sitting unread in the file, and the pairing rule was then rebuilt around the
stand-in being unreliable.

Fixed by restoring the client's semantics exactly: construction moved below the
`maxFPS` read, `isMultiplayer` taken from `m_originalGameMode`, `m_skippedOne`
back, FIFO pairing back, and the arbitrary 16-entry queue cap removed (it could
silently drop recorded checksums and misalign the queue on its own). The frame
tags are kept, but only to label the log line. The trace now prints the recorded
game mode and the dropped first checksum, so the next log says which pairing rule
was in force rather than leaving it to be inferred.

What this costs: every desync conclusion drawn from a multiplayer replay before
today has to be re-taken. The `Trig.cpp` fix stands on its own measurement
(x87 at 24-bit mantissa versus bionic over 62801 angles, `sinf` differing on
1.236% of them and `(float)sin((double)x)` on none), but "frame 100 diverges" and
"1000 frames match" were both read through a shifted instrument.

### Also found, not on the critical path

Sweeping the simulation tree against the client with `diff --strip-trailing-cr`
(the file trees differ in line endings, which had been inflating the counts):

- `TurretAI::xfer` is version 1 here and version 2 in the client, which also
  serializes the base `StateMachine`. This affects save games, not the lockstep
  checksum — `XferCRC::xferSnapshot` calls `crc()`, not `xfer()` — so it is a
  real divergence in the wrong place to explain this one.
- `AIStates.cpp` compares `getNumFramesBlocked()` against
  `static_cast<float>(LOGICFRAMES_PER_SECOND)/4` where the client uses integer
  division. At 60 Hz both are 15; at 30 Hz ours is 7.5 and the client's is 7.
  Latent, and only on the 30 Hz engine.
- `Weapon.cpp` is missing the client's `SupW_AuroraFuelBombWeapon`
  `m_dieOnDetonate = TRUE` and its empty-Stinger-Site targeting rule.
- `AIUpdate.cpp` skips an unresolved locomotor name where the client throws.
  Checked against the user's log: no locomotor is missing on their install, so
  this did not fire.
- `StealthDetectorUpdate.cpp` and `ObjectCreationList.cpp` guard
  `createParticleSystem` against a null template where the client does not.
  Particle systems are client-side and absent from the logic checksum.

## 22/09/2026 — The locator's blind spot, and twelve supply piles

Spent the day making the checksum locator say something it could not say before,
and it mostly answered by ruling itself out.

**The backward walk rotates a difference, it does not shrink it.** I had read the
objects-section dump as evidence that the divergence lived inside that section,
because their walked-back value and ours agreed in the top eighteen bits at its end
— a 1-in-262144 coincidence, I thought. It is not: the walk rotates the difference
one bit per word, so a small word difference anywhere in the 79136 words after the
section lands as a small number at its boundary just as easily. Claim withdrawn, and
the dump is now the whole stream instead of one section, which removes the
assumption rather than arguing about it.

**The candidate lists are shorter than they look.** A single difference reappears
with the same implied delta every 32 positions, because that is where the rotation
comes full circle. Four of the twelve candidates at frame 100 were one hypothesis
wearing four hats (`5B030000`, `+859` after the byte swap, at words 5650, 5682, 5714
and 5746).

**The filter that does work: the same implied word at every checkpoint.** At the
true position the implied delta is frame-independent, because both machines agree up
to it. Intersecting eleven checkpoints leaves exactly three positions, and all three
are single elements of a rotation matrix — which cannot differ alone. Together with
the offline searches (transforms at up to three ULP, every three-word run, every
per-object constant offset: all zero hits) that closes the single-word story.

**What is left is an object list that is not the same list.** The failing replay's
map has 172 objects on frame 0 and 166 from frame 100 on, and the six that go are
exact positional duplicates of six survivors — the twelve `SupplyPileSmall` are six
pairs sharing coordinates to the last bit, four of them at (0,0,0). They appear
after the multiplayer starting units, so a script created them, on frame 0. If a
script that should run once runs twice here, every object created afterwards carries
an id the PC client never issued, and scattered single words differ all over the
stream — the one shape every search above is blind to.

So the new instrument is not more arithmetic, it is attribution: object creation and
destruction now name the script responsible, or `engine` when none is running.
`GXTrace` holds the running script's name for the duration of `executeScript`.

Also worth recording, since it cost an hour: the twelve skipped map reveals on that
map are `Player_5_Start` and `Player_6_Start` on a four-player map, and the twenty
others are players that do not exist in a 1v1. The PC client skips them too. Not a
lead.

### Later the same day: two leads closed, one of them mine to own

The attribution trace answered its question immediately, and the answer was no.
The twelve supply piles are the map's own doing: `Setup Scripts` creates six,
`Supply Spawn` six more at the same coordinates, `Supply Remove` destroys the
first six, all on frame 0, all in the map. The PC client runs the same three
scripts.

Then I read the same log's script-evaluation trace and announced that the shell
map's scripts were leaking into the game and spawning 44 phantom objects. They
are not. The shell map behind the main menu is a running game with its own frame
0, 1 and 2, and my trace printed only the frame number, so one log carried three
different games' opening frames under the same label and I segmented them as one.
Scoped to the replay's own lines, its frame 0 evaluates 869 scripts — the map's
852 plus 4, plus 86 from `SkirmishScripts.scb`/`MultiplayerScripts.scb` that the
engine loads by design — and none from the shell map. The traces now carry
`mode <GameMode>`; a frame number in this engine is not an identifier.

What did come out of the full-stream dump is worth more than either lead. A
deletion hypothesis — "we feed words they do not" — is not a search but an O(1)
test: if the two streams agree from word `j` onwards, their value at `j` must
equal ours at `p`. One hash join over all 85538 positions covers every position
and every length at once. Result: one candidate, at chance level (0.85 expected),
spanning 71067 words from the middle of a rock to the middle of the fog of war.
Meaningless. So the streams are not ours minus anything, and with the single-word
and multi-word searches already empty, one 32-bit number per hundred frames has
no more to give.

The next input is the `.rep` file. Nothing yet has checked that the game we build
for playback is the game that was recorded — and playback currently constructs
exactly one real player plus the observer, with one command centre and one dozer.
If the recording had more players, their starting units are words we do not have,
which is the one case the deletion test cannot see.

### The replay header, read at last

The `.rep` carries its `GameInfo` as plain text, so this cost one `head`:
`S=HMYSOREZ,0,0,TT,-1,2,-1,0,1:X:X:X:X:X:X:X:` — one human, seven closed slots,
no AI, and `SD=976636386` for the seed. So the recording had exactly one
participant, and the theory that the PC hashed some other player's buildings and
fog of war is gone.

The seed turned out to be checkable without a PC at all. `seedRandom` and the draw
ladder are twenty lines of integer arithmetic and `GetGameLogicRandomSeedCRC` is a
byte-wise CRC over the six-word state, so a short script can seed with the
replay's number and step. After exactly 109 draws the CRC is `DA7DB690` — which is
what our log prints at frame 0, and the independent RNG tally in the same log says
`draws=109`. The replay's seed reaches the logic RNG intact, the generator is
bit-exact against the PC's, and nothing draws before frame 0. The block that
resolves `startPos = -1` is identical to the client's and, in this game, takes the
branch that draws nothing.

I also checked the one thing I should have checked earliest: whether my own
instrumentation is in the checksum. `AI::crc` is the single `crc()` function that
differs from the client's, and the difference is trace lines and counters — no
`xfer` call added, removed or reordered. Clean.

What the full dump gave, beyond the deletion test, is a much cheaper unit of work.
A hypothesis confined to a window `[a, b)` costs `b - a` steps, not a pass over
85538 words, because their value at `b` is already known from the inverse walk and
needs no assumption about the rest. Twelve steps to test a transform. That made two
sweeps exhaustive: every object's twelve transform words at up to three ULP (4.8M
tests) and every plausible pair within 32 words (172M tests). Both zero.

Without the plausibility filter the pair sweep returned 23 hits where chance
predicts 0.05 — every one of them demanding that a structurally-zero word become
`0xFFFFFFFC`, or a byte inside a weapon's name change. That is the shape of the
remaining problem: a two-word hypothesis has one equation and two unknowns, so one
checkpoint admits a solution nearly everywhere. The fix is not a better filter but
more constraints, so the dump now fires on three checkpoints rather than one; a
static difference must hold at the same positions with the same deltas on all
three, and three 32-bit constraints are more than a pair of positions and deltas
can fake.

### Three checkpoints: 92% of the checksum cleared, by proof

The three-dump build paid off on the first log, and the first thing it showed was
about our side: between frames 100, 200 and 300 exactly one of our 85538 words
changes -- word 6402, the logic RNG state. The dozer has arrived; nothing moves.

That turns the PC's three checksums into a proof. Walk each one back through our
words to the end of the objects section: every walk crosses 79135 suffix words --
fog of war, player list, AI -- plus a different RNG word each time. If any of that
differed from the PC, three walks would land on three unrelated numbers. All three
land on `3091A52B`. So the fog of war, the player list and the AI are identical to
the PC's, the RNG state is identical on all three frames (our word is reached from
the replay's seed after exactly 310, 510 and 710 draws), and the whole difference
sits inside the 6402-word objects section, which is static from frame 100 on.

It also exposed a blind spot I had built myself. "The PC made a different number
of RNG draws" had never actually been tested, because both of my filters were
structurally unable to accept it: one demanded the same implied word on every
checkpoint, and an RNG word changes every frame; the other looked for fields
differing by a few units, and an RNG state CRC is an arbitrary number. Tested
directly now, and closed -- but the lesson stays: a filter that rejects every
candidate has to be checked for whether it could have accepted the right one.

Inside the objects section the three checkpoints add nothing, because the section
is identical on all of them -- one equation, however many times it is measured.
3.3 billion window tests of integer-shaped differences (single stamps, pairs moved
by the same delta, triples) produced only aliasing near the section's end and
nothing in its first 130 objects, the dozer included. I also checked the one line
in `Object::crc` that looked platform-shaped -- the upgrade mask fed in as
`sizeof(BitFlags<512>)` bytes of raw memory -- and it is 64 bytes on both MSVC and
libc++.

So the next step needs the user's PC, not mine: one solo recording on the same map
with no commands at all, and one with a single short dozer move. The first says
whether the initial state is right; the pair isolates what one move leaves behind.

### A second recording, and a candidate with a name

The user recorded a second game on the same map, the dozer driving and building
this time. The three-checkpoint proof held again -- difference inside the objects
section, everything after it identical to the PC -- and at frame 100 the two
recordings' objects sections differ in exactly two words, the dozer's X and Y. So
any static explanation has to reproduce both recordings: a second equation.

It finally put Gemini's terrain-height idea to a real test, and in doing so showed
my earlier sweep never had. The dozer sits at 1905, 1845, 30 -- exact integers --
and the filter that limited ULP perturbations to "arithmetically derived" elements
had skipped all three. Tested properly now across both recordings (position,
height, another pathfind cell, facing angle; 370M tests): nothing.

Then the natural family of multi-object differences -- one field of every object
of one template, shifted by the same amount. 406016 hypotheses, two survivors,
both on the map's two `AmericaCheckpoint` objects, both matching both recordings
to the bit. One says m22 is `0.99999994` on the PC where we have `1.0`: a one-ULP
difference in a computed rotation element of a quarter-turned object, which is
exactly what x87 and ARM disagree about. The other is its alias -- a zero word two
units low -- and it fits the same two recordings equally well.

Two recordings cannot separate them, and I measured why rather than assume it: a
static change produces the same end difference in both only when it sits in the
last few hundred words, and the checkpoints are right there. What separates them
is a checkpoint with a different object list, because that changes every running
value the difference passes through. The second recording has seventeen of those
after the building goes up. So the engine now tests each hypothesis itself at
every mismatching checkpoint and prints whether the result is the PC's number.
Same recording, one more run, and the candidate either holds seventeen times or
falls over.

Also corrected in the lesson: I had written that the start-position block draws
nothing from the RNG "which the tally confirms". It draws twice. I had printed
seven of the tally's eleven lines.

### Found it: the PC's Inv_Sqrt is an approximation

A stock map where the user just stood still matched the PC at every checkpoint.
It had no `AmericaCheckpoint` on it -- and the Casino arithmetic had just named
exactly those objects as the difference: both carrying m22 = `0.99999994` on the
PC. The dozer's start-of-game snap to a cell centre happened on the stock map too
and matched, so movement was out.

Then the game data said why only those two. `AmericaCheckpoint` has
`STICK_TO_TERRAIN_SLOPE`, so its matrix is built by `alignOnTerrain`, whose third
column is the terrain normal. The other quarter-turned objects on the map don't
have the flag. The normal comes from `Normalized_Cross_Product`, which calls
`WWMath::Inv_Sqrt` -- and `wwmath.h` gives that function an `_M_IX86` branch in
inline x87 assembly: a magic constant and three Newton steps. The PC client is
32-bit MSVC and takes it; we took `1.0f / sqrt()`. On flat ground the cross
product is `(0, 0, 1024)`; exact normalisation gives 1.0, the x86 routine gives
`0x3F7FFFFF`. Emulated under the client's `_PC_24`/`_RC_NEAR` that is precisely
the value the checksum inversion had demanded.

Checked on real x87 before touching the build: the portable replacement matches
x87 arithmetic at `_PC_24` over 6.9 million inputs with no differences, and a full
emulation of the checkpoint matrix before and after reproduces our dump and then
exactly the PC's. The same `#if` hid three more: `Float_To_Long` rounds on the PC
and truncated here, and `Sin`/`Cos` are `fsin`/`fcos` there and were `sinf`/`cosf`
here. All four now do what the reference does. `Inv_Sqrt` sits under every
`Vector3::Normalize()` the locomotor, physics and dozer AI use, which is also why
the second recording drifted further once the dozer started driving.

### The fix holds, and the second cause has the same shape

On the device: the idle Casino recording now matches the PC at all eleven
checkpoints, and the driving one matches at 100, 200 and 300 -- it diverged at 100
before. From 400 it still diverges, which is exactly when its dozer starts to
drive and turn.

Four full dumps of those later frames showed nothing in the objects section
moving except the dozer, and no ULP-sized drift of its transform reproducing the
PC's number -- expected, since a movement difference compounds. So instead of
searching the numbers I looked for the next class of "same source, different code".

The reference is built `/arch:SSE2`, so its ordinary float and double arithmetic is
IEEE SSE like ours; only explicit x87 assembly differs, and every other `__asm` in
Core turned out to be dead code. But 32-bit MSVC's CRT has no float
transcendentals at all -- `sinf(x)` is an inline `(float)sin((double)x)` -- while
bionic's `sinf` is a separate algorithm differing on about 1.2% of angles. And the
WWMath rotation helpers call `sinf`/`cosf` directly, twenty times over, and
`PhysicsUpdate` rotates a moving unit's matrix with them every frame. They now go
through `WWMath::Sin/Cos`. Unlike the first fix, this one cannot be checked
against a frozen value beforehand -- the effect compounds along the path -- so the
same recording decides.

### The rotation fix did nothing; the real one was hiding behind overloads

The Sin/Cos change in the WWMath headers produced exactly the same checksums at
400, 500 and 600 as the build before it -- bit for bit. So it touched nothing in
that window. Correct, harmless, not the cause. Comparing a new build's numbers
with the old build's is the cheapest measurement there is, and it said so at once.

Parsing `8.rep` itself gave the timeline: the dozer is selected at 262, the build
order for a power plant arrives at 342, and the first mismatch is 400. So whatever
diverges, it diverges while the dozer steers towards the site.

My first sweep for float transcendentals grepped for `sinf(`. But in C++ a plain
`atan2(dy, dx)` with `Real` arguments resolves to the float overload -- the NDK's
clang compiles it to `b atan2f` -- and nothing in the source says so. There are 59
such calls in the simulation directories, eight of them in `Locomotor`'s steering,
all going to bionic's single-precision routines while the PC client evaluates them
in double and rounds once. Instead of editing every site and missing the ones hidden
behind templates, the binary now carries its own `sinf`, `atan2f` and the rest with
the reference's semantics and hidden visibility, so every internal call binds to
them. The object file was checked before building; after building, `libmain.so`
must stop importing those names from libc.

### 7 and 8 match; 1.rep breaks at a cancelled barracks

The two maths fixes hold: the idle and the driving Casino recordings now match the
PC at every checkpoint. Then the user ran `1.rep`, Tournament B against three easy
AIs, and it matched for 1900 frames and broke by 2000. The replay's own commands
put a cancelled barracks at 1908. The RNG tally says what that set off: 39 pieces
of `GenericDebris` flung out with random spin and force, each one killed when it
comes to rest.

The checksum arithmetic ruled out the easy answer. No object's transform, nudged
by up to 8 ULP in any three words or 64 ULP in position, reconciles the stream --
1.2 billion windows tried. So it is not one piece rounding differently. It is more
objects, a different set of them, or a different number of random draws, and one
checksum per checkpoint cannot tell those apart.

The code comparison turned up something worse than a rounding difference: the
simulation reads the frame rate. The debris OCL asks the dynamic LOD whether to
skip a piece, slow deaths ask it how fast to run, and the display resets that tier
from the measured FPS every frame -- on the PC client too. Two peers on opposite
sides of a threshold play different games, and phones are the ones that drop. In a
multiplayer game or a replay the logic now reads the VeryHigh tier regardless;
particles and shadows still scale. The phone played this replay at 60 fps, so this
almost certainly does not change its outcome; a new trace line now prints the tier. It is a real fix, not this replay's answer. The next
two inputs decide it: `1.rep` played back on the PC itself, and a quiet Casino
recording with one cancelled building.

A cancellation is a destruction. `onDozerCancelConstruct` refunds the cost and calls
`kill()`, which is ordinary full-health damage. From there it follows the same path
as a building destroyed in battle: die modules, debris, slow death. So the LOD pin
covers destroyed buildings and dying units in general, and whatever still differs in
`1.rep` would appear on destruction too. The one difference left is the death type.
A cancellation dies as `DEATH_NORMAL` from nobody, while combat brings its own damage
type, which picks the die modules. Once a cancel recording matches, a building
destroyed by weapons is the next thing to check.

### The LOD theory, measured and dead

The new build's first log settled it within the first 20 lines of the game: all four
dynamic LOD tiers in this `GameLOD.ini` carry the same logic values: no debris
skipped, slow deaths at full length. The INI checksum matches the PC's, so the PC
has the same table. The frame rate cannot touch the simulation here, and `1.rep`
diverged at 2000 with a checksum identical to the previous build's. The pin stays
as a harmless guard against mods, but it explains nothing. I should have printed
the tier table before theorising about thresholds.

I also checked whether the PC client really sees its own 60 Hz and community-patch
switches everywhere, since it defines them in a header rather than in CMake. It
does: `GameCommon.h` includes that header, and everything in the simulation includes
`GameCommon.h`. The phone side has nothing left that one checksum per checkpoint can
decide. It needs the PC: `1.rep` played on the PC, and a quiet cancel-a-building
recording.

### What MSVC and clang may compile differently, checked

With the libm functions settled, the only room left for a divergence is where the
two compilers may legally differ. Argument evaluation order: no statement in the
simulation draws the logic RNG twice. Uninitialised locals: 442 simulation files
recompiled with clang's uninitialised-variable warnings, nothing on the
debris/death path. The WWMath headers against the client: only our intentional
fixes. On the way, two port changes that renumbered enums: `DAMAGE_FLESHY_SNIPER` and
`KINDOF_AIRFIELD`, both compiled into Zero Hour here and not in the client. There is
also a Generals-era shim that makes every drone `NO_SELECT`. None of them reach the
checksum. The shim does change what a click on a drone turns into, and that is
written down for later. Next, the same route that found the dozer bugs: a quiet Casino
recording with one cancelled building, and one with a sold building.

### The leak in the float fix: sincosf

`ReferenceFloatMath` gives the binary the PC's semantics for `sinf`, `cosf` and the
rest. What I never did was check the finished binary: `libmain.so` still imported
`sincosf` from bionic. No source file calls it. clang merges `sin(a)` and `cos(a)` of
the same float argument into one `sincosf` call, and that call bypasses our
definitions. Four simulation files were affected, and all of them are 1.rep's
territory. `Geometry.cpp`'s box bounds decide which partition cells a building or a
construction site occupies. `BuildAssistant` decides whether and where a structure may
be placed. `AISkirmishPlayer` places the AI's base defences. `AIGroup` sends group
members to formation spots. Both `sincosf` and `sincos` are now defined in
`ReferenceFloatMath.cpp`, so any merge lands on the reference semantics. From now on
the import list is checked after every build.

### libm measured innocent

The math trace came back: 52,824 libm calls in frames 1900-1999, 123 call sites,
**zero** fragile, meaning none close enough to a float rounding boundary for any
accurate libm, the PC's included, to round it differently. Symbolised, the sites are
the ones expected: locomotors, debris physics, collision tests, the debris OCL, and
`findPositionAround` for the GLA worker leaving its building. The normalised code
diff against the client also came back clean for the whole simulation and the terrain
height path. What is left unmeasured is whether the PC reproduces its own recording.

### Same source, different compiler assumptions

With the maths and the code both measured equal, what remains is what each compiler
assumes about undefined behaviour. MSVC: no strict aliasing, signed ints wrap, null
checks after a dereference stay. clang -O2 assumes the opposite, and this engine
reads floats through `*(unsigned*)&f` right inside `REAL_TO_INT_FLOOR`. Compiling
every simulation file both ways changed the machine code of 276 out of 441. The build
now uses `-fno-strict-aliasing -fwrapv -fno-delete-null-pointer-checks`, the reference
compiler's semantics. `1.rep` will say whether any of it was live at frame 2000.

### Found it: NaN to int when debris lands

The user noticed it before the trace did: the mismatch comes when the debris lands,
not when the building breaks. The per-module fp trace then named the culprit:
`PhysicsBehavior` on `GenericDebris`, once per piece, as each came to rest.
`KillWhenRestingOnGround` kills the piece, and `SlowDeathBehavior::getProbabilityModifier`
divides overkill by max health. Debris has 0 max health, so that is 0/0 = NaN, which is
then converted to an `Int`. On the PC's x86 that yields 0x80000000, the probability
clamps to 1, and the following `GameLogicRandomValue(1, 1)` draws nothing. On ARM it
yields 0, the probability stays normal, and a random value is drawn. One extra draw per
piece of debris. The conversion now follows the reference's semantics
(`realToIntTruncRef`).

### The debris fix holds; next stop 4400

`1.rep` now matches the PC up to frame 4300: the debris NaN was it. The next mismatch
is at 4400, right after the user queues Chinooks at the supply center and sets a rally
point. The invalid flag is raised in 36 frames of that window, but the trace only
printed the first of each kind, so it cannot yet say which one is new. It now prints a
per-window table of kinds.

### Five replays in one log: two match to the end, and a new client

The user played five PC recordings back in a single session, and each run was matched
to its replay by its player list:

| replay | client | players | result |
|---|---|---|---|
| `3.rep` | Aug 28 | solo | matches to the end (1000) |
| `4.rep` | Aug 28 | 1 AI | **matches the whole game, to 8500** |
| `5.rep` | Aug 28 | 2 hard AIs | diverges at 3700 |
| `1.rep` | Aug 28 | 3 AIs | diverges at 4400, as before |
| `USA.rep` | **Sep 23** | solo | diverges at 100 |

`3.rep` is the key to `USA.rep`. It has the same map and the same solo start, recorded
on the old client, and it matches. So the frame-100 divergence comes with the client
update. The GeneralsOnline history was squashed on 12/09, and the squash commit is
titled "networking.cmake - optimized target". It adds a `win32-vcpkg-optimized` preset
with `/GL /LTCG /Ob3` and optionally PGO, and it turns the memory-pool debug options
off. Every change after that commit is lobby, score screen and UI. The memory-pool
option turned out not to matter: the debug filler only exists under `RTS_DEBUG`. What
changed between the 28 August build and 12 September cannot be seen in their
repository.

In `5.rep` the frame-3700 window has no NaN, no out-of-range float conversion and no
fragile libm call. Something else did happen in it. A second AI group appears in the
checksum (the AI's own command center, barracks, power plant and dozer). Two
`SpySatellitePing` objects appear too: the AIs fired a satellite scan. Taking the group
out, or rebuilding it with any subset of those members, any id from 55 to 99 and either
dirty flag, does not reproduce the PC's checksum. So the difference is elsewhere, or
not only there.

New instrument: at the first mismatch the engine now compares against the previous
checkpoint, which matched and is still in the capture ring. It lists every object that
changed in between, with its words. It then tests whether undoing any single change,
or any pair of changes, gives the PC's number.

### The new client differs with nobody touching anything

`New_clear.rep`: the 23 September client, solo, no input. It diverges at 100 and stays
at the same pair of checksums through 500. So the new build itself simulates the solo
start differently; the old build did not (`3.rep` matches). The new `crc since`
instrument named the only things that change in those 100 frames: 11 objects snapping
to cell centres, and the seed. No combination of undoing them, no draw count and no
shroud variant gives the PC's number. A static single-word difference shared with
`USA.rep` is ruled out as well. Next: the new exe itself, to disassemble.

### Found: the new client tags its checksum

The user sent the updated GeneralsOnline files, and the data pack manifest led to the
previous version too. Diffing the two exes' checksum functions, located by the
`MARKER:Objects` string, showed that the 23 September client appends
`MARKER:OfficialLogicCRCRevision` and `0x474F0001` to every logic CRC. Adding those
words to our captured stream gives the PC's recorded numbers exactly, for both
new-client replays. The simulation was already identical. `getCRC` now appends the tag.
Replays pick the right variant automatically at the first checkpoint, so recordings
from both clients keep working.

### Revision tag confirmed on the device; next: NaN bits

On the device, `New_clear.rep` now matches to its end, and `USA.rep` matches to 3500
instead of breaking at 100. The next mismatch is Rangers walking, and every diverging
replay shows a NaN inside the Ranger locomotor. Arithmetic treats all NaNs alike, but
`REAL_TO_INT_FLOOR` does not. It reads the sign bit (the default NaN is negative on
x86 and positive on ARM) and then calls `lroundf`, which is 32-bit and returns 0 on the
PC's UCRT, but 64-bit and saturating in bionic. Both now follow the PC, and each call
site that hits the case is printed.

### Replay check in the launcher

Waiting for frame 3600 of a replay was the slowest part of every round. The launcher
now has a Replay check screen: pick a `.rep`, then either **run it through** (simulated
as fast as the phone allows, stops shortly after the first mismatch and returns with a
one-line result) or **watch from a frame** (fast-forward, then normal playback). A
replay cannot be cut to start later, because it holds commands, not state. So
"starting later" means simulating the start without drawing it.

### Supply replay: the drone, not the Chinook

The shroud trace explained every fog change between 5800 and 5900: six delayed unlooks
from the Chinooks' earlier flight, identical here and on the PC. The full checksum dumps
at 5900, 6000 and 6100 then pointed somewhere else. At all three, the PC's number is ours
with just the **spy drone's height** a few ULP off (−2, −6, +6). Both Chinooks, flying
off after the delivery, match exactly. The drone's hover is now modelled offline. Our
own 697 traced frames come out bit for bit, as a 78-frame limit cycle. None of the
obvious differences (lift, gravity, terrain height, the velocity clamp, float evaluation
order) reproduces the PC's values. The next build prints the per-frame physics and lift
inputs, and every command the replay executes, around the mismatch.

### The control replays: supply, not the drone

A drone-only replay matches for 14500 frames, and a supply-only replay diverges at 3600,
during the free Chinook's first flight from the warehouse to the supply centre. The user
was right. The earlier "drone height" reading was an artefact of the checksum's
arithmetic: a multi-word difference can masquerade as a one-word one. The Chinook's
approach alternates between braking and accelerating frame by frame near its goal, and
the docking points it flies to are read once, on first use, from the building model's
bones on the render side. The next build prints those dock points and every movement
decision.

### The Chinook's approach, modelled to the bit

The docking point read from the supply centre model is identical to what the PC would
compute. The files are the same, and so are the float code and the libm path. The
Chinook's flight from the warehouse to the dock (3501..3700) is now reproduced exactly by
an offline model built from the new trace. The PC's checksum says its Chinook is a few
ULP off in position from frame 3592. No single changed number or single disturbance
explains both the 3600 and 3700 checkpoints, so what the PC does differently in this
flight is still open.

The supply centre's dock bones, recomputed from the model file with the engine's own
arithmetic, match the phone bit for bit. The docking geometry is closed as a suspect.

### Letting the PC find the frame

The new client's machine code for the whole Chinook flight matches the public source,
and no libm result on it is anywhere near a rounding boundary. So the PC differs in an
input, and checkpoints 100 frames apart cannot say which. The replay header's checksum
interval now does that job. The launcher can log the checksum of every frame, and a
script writes those into a copy of the replay with interval 1. Played on the PC, that
copy stops at the first frame where the PC disagrees with the phone.

The first attempt logged nothing, because the switch turned itself off when the game
started. The option is now remembered by the launcher. It is also passed as a marker
file in the game folder, which the engine reads in the same way as `gx_net_trace.txt`.
The replay-check start line now says whether the option is on, so a log shows at a
glance whether it took effect.

The second attempt showed the option as on, but still no per-frame lines. The condition
covered only solo games, and the supply replay is a multiplayer recording. It now covers
both.

### The PC names the frame: 3594

The copy with a checksum on every frame matched itself on the phone at all 4299
checkpoints. On the PC it stopped at `Frame:3594` (PC `BD836F63`, phone `240D6FC1`). So
the two machines agree bit for bit on every frame up to and including 3593.

A frame's checksum is taken after scripts and terrain but before the objects update.
So the difference is made while objects update in frame 3593. On the phone that is
exactly one event: after one idle hover frame (3592, which leaves the checksum
unchanged), the Chinook gets its final goal, the dock point (1538.53, 694.08), and takes
its first turning, braking step toward it. The next step is a phone replay that carries
the PC's value at 3594 only, so the phone dumps that frame's checksum words and the
single frame can be solved against the Chinook model.

The dump of frame 3594 puts the difference on the Chinook's position: exactly 30 ULP in
y, or x and y together, with the heading unchanged. The model rules out any single
rounding in the first step toward the dock, and a one-time position offset alone.
Something keeps differing in the frames after it. So the PC is now asked for its
checksums of 3595, 3596, and so on, one frame per run. The phone copy that carries the
PC's value at 3594 is exactly the file that takes the PC past 3594.

### It was never the helicopter

The PC's checksums for 3594 and 3595 differ from the phone's by the same amount. A
moving object cannot do that, so the difference is a field that stays fixed. The
region where the two frames agree narrows it to the dozer 319 and power plant 318
entries. In that region the only natural reading is one bit in the power plant's
upgrade mask: **Advanced Control Rods**.

The replay queues that upgrade at frame 1794. Its build time is 30 s, which is 1800
frames at 60 Hz, so the PC completes it at exactly 3593. The phone never completes it.
The command names the upgrade by its *name key*, a number handed out in the order names
are first registered. Upstream only aligns that numbering with the retail client under
`RETAIL_COMPATIBLE_CRC`, and that switch is off for both clients. The new build logs this
device's key for the upgrade and what it makes of the PC's key 2265.

The divergence is in the source: this port's GUI function tables carry ten names the
PC client does not (the Extras menu, the group panel and a credits draw callback). They
are registered before the upgrade store, so every upgrade and science key on the phone
was ten higher than on the PC. Those ten names are now keyed after the stores and
before any window is loaded. A build is correct when its log reports Control Rods as
key 2265.

### Confirmed

With the name-key fix, `USA_Supply_Clear.rep` matches the PC on all 88 checkpoints.
`USA.rep`, which used to break at 3600, now matches to 16700. Its next divergence comes
right after a factory produces an Avenger (template id 228, queued at 15950, used for
the first time in that replay). Object creation is now traced for the whole replay, so
the next log shows when that unit appears. The lesson has a new section: the whole path
from the Chinook to the name key, and which step removed which guesses.

`USA.rep` now diverges when an Avenger leaves the factory, at 16745 on the phone. It
took 795 frames against an INI build time of 600, which is the low-power penalty. Energy
is not in the checksum, so the log now records every change to a player's energy.

The PC stops at 16746 on the every-frame copy of `USA.rep`: everything agrees until the
phone's Avenger leaves the factory. Production and disable events are now logged, to
see what the extra 195 frames were.

### Second divergence found: the Avenger's turret

When an Avenger appears, the phone's dump holds two identical transforms, the tank's and
its laser turret's. A port-only change from April pinned every Overlord-style rider to its
host's position each frame, working around bone positions that were wrong on POSIX back
then. The PC places the turret at its bone, and that position is in the checksum.
`OverlordContain` in Zero Hour is back to the GeneralsOnline client's version.

With the Overlord fix, `USA.rep` matches to 27200. The next divergence is at 27300, in
the player's battle-plan bonuses, just after the Strategy Center switched plans at
27298. An every-frame run will name the exact frame.

### Third divergence: a KindOf index

The PC names 27260, right as Hold the Line comes up. The battle-plan bonus hashes two
KindOf masks raw, and the PC's bit was one position lower than the phone's. A July change
enabled `KINDOF_AIRFIELD` for Zero Hour in the middle of the enum, which moved every later
KindOf up by one. That change also made drones `NO_SELECT`. The enum, its name table and
the shim now match the PC client's. A sweep of all engine enums against the PC client found
no other simulation-relevant difference.

### `USA.rep` matches the PC end to end

With the name-key, Overlord and KindOf fixes, the 30800-frame `USA.rep` matches the PC on
all 308 checkpoints. The method that found all three is now at the top of
`LESSON-cross-play-desync-method.md`: the loop, how to read a dump, what not to do, and
the tools. In short, every divergence was this port differing from the PC client's source,
not floating point.

### Fourth divergence: a jet's takeoff one frame late

In the new `Global_War.rep`, the PC's checksums at 19158 and 19159 are the phone's state with
the stealth fighter one frame further along. An upstream tweak (#1297) makes a jet on an
upper parking space skip its first runway reservation. The PC client does not have it, so
the phone's jets took off a frame late. The file now matches the PC client's. A sweep of
upstream blocks against the PC client found no other simulation change that is not
already aligned.

### Fifth divergence (open): six power plants at frame 19250

After the runway fix, `Global_War.rep` matches up to frame 19250 and differs at 19251 (PC
`C953185B`, 19252 `0DD284CE`). During frame 19250 on the phone, AI player 3 gains +15 energy on
each of six power plants, which ends a long power shortage and re-enables its Patriots and
particle cannons. No single word explains the PC value, and energy and upgrades are not in the
checksum. The power, upgrade and production code is identical to the PC client's, so the
suspect is when the research finished rather than how. The AI queues upgrades without a
network message, so the phone now logs `upgrade queue` (with the money left) and `upgrade
done`. A replay named `..._upgshift<N>at<F>.rep` (N may be `m<N>` for negative) makes an
upgrade that would finish at frame F finish N frames later, so one phone run tests "the PC
finished it on another frame" against the PC's own checksums.

The four upgrade-shift runs ruled out a timing shift of all six plants. The subset search
found the answer instead: the PC's 19251 matches exactly when five plants have the upgrade
and plant 864 does not. Plant 864 was placed at 17450 and never finished building, yet the
AI queued the research on it at 17451 and the phone ran it to completion. The knob now takes
an object id (`upgshift<N>at<F>id<ID>`), the queue trace says whether the building was under
construction and which script queued it, and a trace reports teams whose owner only the
port's name fallback in `TeamFactory::initTeam` resolves.

The PC's own `Global_War.rep`, with plant 864 never finishing its research, now matches to
24200 instead of 19200; the next mismatch is the knob finishing it at 24250. The research was
queued by the skirmish script `USA Power Critical - H`, and every function on its path is
identical to the PC client's, so the mechanism is still open. The sweep did find one real
upstream divergence: TheSuperHackers #2129 changed sequential-script spin detection from the
script pointer to the vector index. It is reverted to the client's pointer comparison.

### Sixth divergence: `itoa` wrote nothing on Android

Both plant-864 variants matched the PC's `Global_War.rep` to 27400 and then disagreed even with
each other at 27500. The only difference between the two runs was the position of two
Overlord-style gattling cannons put on Emperor tanks at 27496: their matrices were stack
garbage. `OpenContain::putObjAtNextFirePoint` builds the bone name `FIREPOINT0` + `itoa(n)`.
The port's `itoa` wrote through `std::stringbuf::pubsetbuf`, which libc++ ignores, so the name
was garbage, the bone lookup failed, and an uninitialized `Matrix3D` went into the rider's
transform and the checksum. `itoa` is now a plain MSVC-compatible implementation. It also
fixes exit-path bone names for units leaving buildings and transports.

### `Global_War.rep` matches the PC to the end

With the `itoa` fix and plant 864's research refused, the PC's `Global_War.rep` (81100 frames,
a match against three AI players) matched on all 811 checkpoints. The refusal is now permanent:
`ProductionUpdate::queueUpgrade` does not queue an upgrade on a building under construction.
Players cannot reach that through the UI, only scripts and the AI can. It is a measured rule
(the client source does not explain it), and the code comment says so.

### Housekeeping: old APKs removed from `apk/`

49 diagnostic and older APKs are removed from the working tree (they stay in git history).
`apk/` keeps only the current build, `GeneralsXZH-android-globalwar-match.apk`. From now on a
new build replaces it instead of adding another 60 MB file next to it.

### Proactive audit against the PC client

The port's compat shims, the allocator, unstable sorts, hash containers, and a normalized diff
of every game-logic file against the GeneralsOnline client were all checked. Fixes:
- `PartitionSolver` sorts like MSVC (`std::stable_sort`, identical up to 32 elements);
- a spent minefield dies through its normal death path, as on the PC;
- `DumbProjectileBehavior` gets the client's null check.

Failed bone lookups that leave stack garbage in a position or matrix are now traced.

### Faster replay checks

A replay check of the 81100-frame `Global_War.rep` wrote a 60 MB log, and 627 302 of its
~670 000 lines were `disable frame`: dead hulks and debris re-assert `DISABLED_HELD` every
frame, and the trace logged each call. It now logs only real changes. The fast-forward loop
also ran only 40 ms of logic between rendered frames; it now runs 250 ms, so the screen still
updates about four times a second and most of the time goes to the simulation.

### `Global_War2.rep`: the community-patch switches

The new all-factions recording diverged at 5771, right after the first poison kill. The PC
client compiles `GameDefines.h` with `GENERALS_ONLINE_COMMUNITY_PATCH_CHANGES` visible (through
`GameCommon.h` -> `WWCommon.h` -> `NextGenMP_defines.h`). So poison kills give experience,
the Battle Bus death fix is on, and so is the drag-selection occupant fix. A September note had
assumed the opposite. Zero Hour now takes the client's arm. Two more configuration
differences were fixed in the same pass:
- INI numbers are parsed with `sscanf` like the client, not `from_chars`/`strtod`;
- `DAMAGE_FLESHY_SNIPER` is Generals-only again, so damage-type indices match the client.

### `Global_War2.rep` matches the PC to the end

With the community-patch switches, INI parsing and damage types aligned, the all-factions
recording (194600 frames, every superweapon) matched the PC client on all 1946 checkpoints.
The replay check took 497 s. No `bone missing` or `partition solver` warnings were logged.

### Base-game tokens in Zero Hour data, without shifting enums

Issue #2 (EA Deluxe Edition data) needed `DamageType = FLESHY_SNIPER` to load; its tester
confirmed that in July. It was then added to the Zero Hour enum, which shifted every later index
away from the PC client's; today's cross-play fix took it out again, which would have broken that
install's startup. `INI::scanIndexList` now reads it as `SNIPER` in Zero Hour (logged once), so
that data loads and the enum stays the client's. A `KindOf = AIRFIELD` alias was added in the same
commit and removed again: nothing shows that install needs it (the July `KINDOF_AIRFIELD` change
did not fix that tester's crash; a missing locomotor did).

### Issue #22: Android 10 storage

Android 10 has no "All files access", and with targetSdk 29+ it applies scoped storage unless
`requestLegacyExternalStorage` is true, so the game folder could not be read even with the Storage
permission granted. The flag is now true (Android 11+ ignores it). On Android 9–10 the folder
picker now asks for the Storage permission instead of "All files access", and a permission denied
with "Don't ask again" opens the app's settings page instead of leaving the user stuck.

### Issue #20: HUD inside the screen's safe area

The GenTool-style corner HUD (FPS and clock at the top left, the match timer at the top right)
and the GeneralsX credit line at the bottom left were clipped by display cutouts and rounded
corners. The launcher now measures the window's safe insets (the cutout on Android 10+, and on
Android 12+ about 0.3 of each rounded corner's radius) and passes them as `-gxSafeInsets
left,top,right,bottom` fractions. `Common/GXSafeArea` turns them into display pixels, and the four
HUD items start inside that area. Client-side only; nothing reaches the simulation.

### Steam release: the "Custom Mission" button

The 2023 Steam release adds three small archives: `PatchWindow.big` (a `MainMenu.wnd` with a
`ButtonCustomMission` in the single-player drop-down, and its own `MapSelectMenu.wnd`),
`PatchINI.big` (the transitions for both) and `PatchData.big` (`Data\Patch.str`, English only:
`GUI:CustomMission` = "CUSTOM MISSION", `GUI:StartCustomMission` = "START GAME"). Only the
executable shipped with them read the `.str` or handled the button, so on this engine it showed
`MISSING: 'GUI:CustomMission'` and did nothing when pressed.

- `MainMenu.cpp` handles the button the way it handles Skirmish: reverse the patch's
  `MainMenuSinglePlayerMenuBackCustomMission` group and push `Menus/MapSelectMenu.wnd`. That
  menu's callbacks (`MapSelectMenu.cpp`: single-player map list, AI difficulty, a
  `GAME_SINGLE_PLAYER` new game) were always in the engine, just never reachable.
- `GameTextManager::fetch`, after every loaded table misses, now tries a small built-in table:
  `GUI:StartCustomMission` is aliased to the game's own `GUI:StartGame` (translated in every
  language the game ships in), and `GUI:CustomMission` has its own text in the eleven text
  languages the launcher offers. A language pack that carries the label still wins.
- `GameWindowTransitionsHandler::reverse` no longer dereferences a group the loaded INI does not
  define (a `PatchWindow.big` copied without `PatchINI.big`); it logs and skips.

### Issue #25: force attack and waypoints on a touchscreen

A mouse player force-attacks with Ctrl+click and queues waypoints with Alt+click; a finger has
neither key, so artillery could not be aimed at a point ahead of a moving target. Two buttons now
appear on the command bar (touch builds only) and switch the same InGameUI flags those keys do:

- **Force attack** (`GUI_COMMAND_GX_FORCE_ATTACK`, the game's attack-cursor art `SCCAttack`) arms
  one order: the next tap goes through `evaluateForceAttack`, as a Ctrl+click does, so your own
  units and bare ground are valid targets; then it disarms. Pressing it again, or the cancel
  gesture, disarms it without an order.
- **Waypoints** (`GUI_COMMAND_WAYPOINTS`, the rally flag `SSRally`, and the game's own
  `CONTROLBAR:Waypoints` / `CONTROLBAR:ToolTipWayPoints` strings, which every language already
  has) stays on until pressed again; every order meanwhile is `MSG_ADD_WAYPOINT`.

Both are drawn pressed while their mode is on. They take a slot the selection's command set leaves
empty (slot 12, between Attack Move and Guard, in 123 of the 137 stock sets with Attack Move; then
leftwards from 10), never a transport's passenger slot, and never cover an existing button. The
orders are the ordinary `MSG_DO_FORCE_ATTACK_*` / `MSG_ADD_WAYPOINT`, so skirmish, campaign, LAN,
online and replays see nothing new. Not built or tested on a device yet.

### Port strings moved out of the engine, into `languages/`

The Custom Mission and force-attack strings were first a table compiled into `GameText.cpp`,
which meant a new language needed a C++ change. They now live where every other translation
does, as plain text: `languages/<language>/generalsx.str`, eleven languages, bundled in the APK by
`package-android-zh.sh` and installed on every launch as `data/<language>/generalsx.str` (always
overwritten, like `Window/GroupPanel.wnd`; a player's `generals.str` is never touched).
`GameTextManager` reads that file for the text language, then the English one, after every table
the game loads; `parseMapStringFile` now takes the target table as a parameter, and the port
loader counts the entries it actually parsed (`getStringCount` pads by 500). Only the alias
`GUI:StartCustomMission` → `GUI:StartGame` stays in code. The launcher's pack download treats a
`languages/` folder with no `generals.str` as "not a pack" instead of a failed download.

### Force-attack button: only for things with a weapon

On a device the button appeared on a dozer. `Object::isAbleToAttack()` answers "is anything
stopping this from attacking now" and is true for an object with no weapon at all (and false while
a dozer builds, which is why it came and went). The button now needs a damage weapon on the object,
or on a passenger its container lets fire out (garrisons, Overlord bunker, Humvee).

### Back to one file per language

The per-language `generalsx.str` from the previous entry was one file too many: a language pack
is `languages/<language>/generals.str`, and a player adds a language by adding that one folder.
The three port labels are now at the end of the Russian and Ukrainian packs, the engine keeps only
their English text as the last fallback (plus the `GUI:StartCustomMission` → `GUI:StartGame`
alias), and `generalsx.str` is gone from the repository, the APK, the launcher and the engine.
The launcher removes what the one build that shipped it installed: every
`data/<language>/generalsx.str`, and a folder only if that leaves it empty.

### Game text: Russian pack completed and checked against the English

The community Russian pack had 3991 of the 6425 labels in the current English data: no mission
subtitles at all (2455 `DIALOGEVENT` lines), 203 lines still in English, and insertions the
original never had — "ЛОКАЛИЗАЦИЯ 2003 SIBERIAN STUDIO" on the loading screen and the
translator's own credit lines. It was reviewed chunk by chunk with
`scripts/language/translate_kit.py`: good lines kept, missing and English lines translated with
the pack's own unit names, insertions removed. 22 labels that exist only in older game data are
kept (with English reconstructed from sibling labels), so the pack serves old and new installs.
Result: 6447 labels, every chunk passing the kit's checks (labels, printf arguments, hotkeys,
subtitle markers). The kit's printf check no longer treats "25% F" as a conversion, and a
line-break difference is a warning, not an error. The other launcher languages are in progress
in `tools/translation-work/`.

Follow-up the same day: the faction is now "GLA" throughout the pack, as in the English and
the Ukrainian pack (the old community pack said "МАО", 283 lines; the one spelled-out name
became "Глобальная Армия Освобождения"), at the owner's request. 66 more lines differed from the English only in
whitespace and slipped past the "still English" filter — the EVA lines of China mission 2 (Hong
Kong), the `MAP:*Temp` mission names, 45 `HELP:` hints and four tooltips — now translated in the
pack's style ("ПОДСКАЗКА:", "Потребление энергии:", "Время для готовности:"). The pack's unit
names ("Улитка" for the Helix, "Молекулярная пушка", "Гнездо стингеров") are the ones the
community uses, checked against a player slang glossary.

## 26/09/2026 — The patch-1 launcher theming lands on the Mod Manager branch

### What came over, and why not a merge

`redaF12-Tech-patch-1` carries three launcher-side feature sets built on top of the
same base this fork's `main` had already moved past: the Daylight/Darkmode switch
(`claude/daylight-darkmode-launcher-ui`, PR #2), the six interface accent colours
plus the theme-tinting of every control (`claude/accent-mode-switch-fix`, PR #4),
and the configurable touch hotkey overlay (`TouchControlsActivity`,
`TouchControlConfig`, `HotkeyOverlayView`). The branch itself is behind `main` on
everything else -- its tip still predates DataPackInstaller, the network
diagnostics screens, the replay check screen, and the 30/60 Hz launcher setting --
so a plain `git merge` of the tip would have deleted all of that (~215k deleted
lines in the three-dot diff). The C++ mod-loading delta it carries is a subset of
what the Mod Manager branch already has (ours also gates the community-patch
board and logs the readable-path warning), so nothing was taken from C++ at all.

Instead the feature files that our branch never touched since the shared base
(`4bba2494`) were carried over wholesale -- `ThemeHelper`, `UiKit`,
`TouchControlConfig`, `TouchControlsActivity`, `HotkeyOverlayView`,
`ic_gzh_daynight`, and the whole colours/themes resource set (`colors.xml`,
`accent_colors.xml` + night variants, `themes.xml`, `themes_accents.xml`,
`touch_controls_colors.xml`, `values-night/`) -- and `gzh_dialog_background.xml`
was dropped because the new themes no longer reference it. `SetupActivity` was
merged by hand: the Mod Manager card, `PREF_MOD_PATH`,
`pendingStoragePickerRequest` (which is what makes the storage-permission
round-trip resume into the mod picker) and the sim-rate section all stayed, and on
top of them came the Appearance card (mode + six-accent strip), the touch-controls
card, and every accent-aware colour read (bottom nav pill, UI-scale slider,
dxvk field stroke, chips, `MaterialAlertDialogBuilder` dialogs,
`ThemeHelper.wrap(LocaleHelper.wrap(...))` in every launcher activity).

### A latent crash fixed on the way in

`TouchControlsActivity` reads four strings unconditionally:
`touch_border_color`, `touch_border_color_help`, `touch_text_color`,
`touch_text_color_picker`. In the patch-1 tree they exist **only** in
`values-ar` -- every non-Arabic device would have hit
`Resources$NotFoundException` the moment the button editor opened. The English
defaults now live in `values/strings.xml`; the Arabic lines come over unchanged.

### Verification without a build

No JDK/SDK in this workspace, so: every Java file the merge touched is
brace/paren balanced; all 23 XML resource files parse; a script resolves every
`R.string/color/dimen/drawable/style` reference in the ten affected activities
and helpers against the actual resources on this branch (all resolve; the two
"missing" hits are `android.R.*` system IDs); the method inventory of
`SetupActivity` is a superset of patch-1's (nothing of theirs lost) plus our five
mod/sim-rate/diagnostics methods; and `git diff --check` is clean. The Android CI
build on a PR toward `main` remains the real compile check.
